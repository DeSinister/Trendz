<h1 class="jumbotron">Edit Category</h1>

<div class="container">
  Current Name: <%=@category.name%>
  <%= form_for @category, :url => update_category_path(@category), method: :patch, class: "form-horizontal" do |f|%>
    <%= f.text_field :name%>
    <%= f.button :submit%>
  <%end%>
  <%= link_to 'Back', categories_path%>
</div>
